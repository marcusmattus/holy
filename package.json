{
  "name": "holy-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo for Holy by Holystic Labs — vibecoding platform powered by HolyOS",
  "homepage": "https://holy.holysticlabs.com",
  "repository": {
    "type": "git",
    "url": "https://github.com/holysticlabs/holy.git"
  },
  "scripts": {
    "build": "turbo build",
    "build:packages": "turbo build --filter=holyos-sdk --filter=@holyos/* --filter=create-holy-app",
    "build:holy": "turbo build --filter=holy",
    "dev": "turbo dev",
    "lint": "turbo lint --filter=holyos-sdk --filter=@holyos/* --filter=create-holy-app",
    "test": "turbo test --filter=holyos-sdk --filter=@holyos/*",
    "test:watch": "turbo test:watch",
    "type-check": "turbo type-check --filter=holyos-sdk --filter=@holyos/* --filter=create-holy-app",
    "format": "prettier --write \"**/*.{ts,tsx,md,json}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,md,json}\"",
    "sdk:generate": "turbo run sdk:generate",
    "changeset": "changeset",
    "ci:version": "changeset version && pnpm install --no-frozen-lockfile",
    "ci:release": "turbo clean && turbo build && changeset publish",
    "prepare": "husky"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.9",
    "husky": "^9.1.7",
    "lint-staged": "^15.5.2",
    "prettier": "^3.3.3",
    "turbo": "^2.3.3",
    "typescript": "5.7.3"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=22",
    "pnpm": ">=9"
  },
  "lint-staged": {
    "**/*.{ts,tsx,js,jsx,json,md}": [
      "prettier --write"
    ]
  },
  "prettier": {
    "semi": false,
    "singleQuote": true,
    "trailingComma": "all"
  },
  "pnpm": {
    "overrides": {
      "esbuild@<=0.24.2": ">=0.25.0",
      "prismjs@<1.30.0": ">=1.30.0"
    }
  }
}
