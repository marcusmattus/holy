# AWS ECS Deployment

- Run web on Vercel and workers/runtime on ECS Fargate tasks.
- Use separate task definitions for runtime, worker pools, and dashboards.
- Use AWS Secrets Manager + Redis with TLS.
