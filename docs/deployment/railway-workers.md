# Railway Worker Deployment

- Deploy runtime service and worker processes as separate Railway services.
- Attach managed Redis and set queue URLs per service.
- Keep settlement and export workers isolated for policy enforcement.
