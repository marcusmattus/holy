# Runtime Service Cluster Scaffold

Provides cluster-ready runtime primitives for:

- `/health`, `/ready`, `/metrics`
- queue and HTTP execution modes
- tenant-scoped execution context
- per-workspace limits and timeout enforcement
- structured logs and graceful shutdown hooks
